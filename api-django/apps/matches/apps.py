from django.apps import AppConfig


class MatchesConfig(AppConfig):
    """Configuración de la app Matches — compatibilidad y conexiones."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.matches'
    verbose_name = 'Matches'
